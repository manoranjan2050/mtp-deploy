<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <!-- Module 4's DatabaseManagerService tests run real CREATE/DROP DATABASE
             statements against this dev machine's actual local MySQL (via the
             mysql_admin connection) - not mocked, since it's genuinely running
             here. Tests use uniquely-named throwaway databases and clean up
             after themselves, same approach as Module 3's temp-directory
             filesystem tests. -->
        <env name="DB_ADMIN_HOST" value="127.0.0.1"/>
        <env name="DB_ADMIN_PORT" value="3306"/>
        <env name="DB_ADMIN_USERNAME" value="root"/>
        <env name="DB_ADMIN_PASSWORD" value=""/>
        <!-- Module 10 (SSL) - this machine's PHP has no working default
             openssl.cnf wired into php.ini, so openssl_pkey_new()/
             openssl_csr_new() fail outright without an explicit config path.
             Not needed on a real Linux CI runner/server. -->
        <env name="MTP_OPENSSL_CONFIG_PATH" value="C:\Program Files\Ampps\php82\extras\ssl\openssl.cnf"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
